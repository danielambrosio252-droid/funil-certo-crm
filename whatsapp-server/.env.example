# ============================================
# CONFIGURAÇÃO DO SERVIDOR WHATSAPP
# ============================================

# Porta do servidor (padrão: 3001)
PORT=3001

# URL do webhook do Escala Certo Pro
# Substitua pelo URL real do seu projeto Supabase
WEBHOOK_URL=https://ysiszrxwbargoyqrrehr.supabase.co/functions/v1/whatsapp-webhook

# Segredo para validar webhooks (opcional, mas recomendado)
WEBHOOK_SECRET=seu_segredo_aqui

# Ambiente (development ou production)
NODE_ENV=production

# ============================================
# CONFIGURAÇÕES OPCIONAIS
# ============================================

# Tempo de reconexão em ms (padrão: 5000)
RECONNECT_TIMEOUT=5000

# Limite de tentativas de reconexão (padrão: 10)
MAX_RECONNECT_ATTEMPTS=10

# Diretório de sessões (padrão: ./sessions)
SESSIONS_DIR=./sessions
